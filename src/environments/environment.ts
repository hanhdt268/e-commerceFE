// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    apiBaseUrl: 'http://localhost:8080',
     firebaseConfig: {
       apiKey: "AIzaSyAotu5tzVuz7eDWSPViRqcUuHdmZE7aOP0",
       authDomain: "graduation-f97dc.firebaseapp.com",
       projectId: "graduation-f97dc",
       storageBucket: "graduation-f97dc.appspot.com",
       messagingSenderId: "1037843523102",
       appId: "1:1037843523102:web:cdc1197833945f8dfcb00f",
       measurementId: "G-KJ6RG7W69J"
    }
  };

  /*
   * For easier debugging in development mode, you can import the following file
   * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
   *
   * This import should be commented out in production mode because it will have a negative impact
   * on performance if an error is thrown.
   */
  // import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
